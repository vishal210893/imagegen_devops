# SPRING PROPERTIES
spring:
  output:
    ansi:
      enabled: always

server:
  port: 8005
  ssl:
    enabled: false
  tomcat:
    max-connections: 50
    connection-timeout: 10s
    threads:
      max: 50
  servlet:
    context-path: /dockerpoc1

logging:
  level:
    root: debug




